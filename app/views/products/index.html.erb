<h1>Products</h1>

<% @products.each do |product| %>
<p>
  <%= link_to *(product.name), "/products/#{product.id}" %>
  <%= product.name %>
  <%= product.description %>
  <%= product.formatted_price %>
<% end  %>
</p>
